#include <iostream>
#include <string>

using namespace std;
int main()
{

  string ingredients;
  cout << "Input the ingredients you have:";
  getline(cin, ingredients);
  
  boolean cake = false;
  
  string butter = "butter";

  if (ingredients.find(butter))
    cake = true;
    
    
   cout << cake;
